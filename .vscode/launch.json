{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Listen for Xdebug",
      "type": "php",
      "request": "launch",
      "port": 9003,
      "hostname": "localhost",
      "pathMappings": {
        // パスは構成に合わせて修正してください
        "/var/www/webroot": "${workspaceFolder}/htdocs"
      }
    }
  ]
}
